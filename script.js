// Start from 10
let a = 10;

// Loop until 'a' is less than or equal to 40
while (a <= 40) {
    // Check if 'a' is even
    if (a % 2 === 0) {
        // Print the even number
        document.write(a + "<br>");
    }
    // Increment 'a' by 1
    a++;
}
